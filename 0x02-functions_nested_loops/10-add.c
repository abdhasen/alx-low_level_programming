#include "main.h"
#include <stdio.h>

/**
 * add - add two numbers.
 *
 * Return: returns sum of the numbers.
 * @a:  first number.
 * @b: second number.
 */
int add(int a, int b)
{
  return (a + b);
}
