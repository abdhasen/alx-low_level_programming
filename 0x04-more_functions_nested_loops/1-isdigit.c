#include <stdio.h>
#include "main.h"

/**
 * _isdigit - print 0 to 9 digits
 * Return: 1 if the character is digit and 0 if it is not.
 * @c: the character to be checked.
 *
 */
int _isdigit(int c)
{
  if (c >= '0' && and c <= '9')
    {
      return (1);
    }
  else
    {
      return (0);
    }
}
