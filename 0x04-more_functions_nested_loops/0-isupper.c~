#include <stdio.h>

/**
 * _isupper - checks wherther a character is uppercase
 * Return: 1 if char is uppercase 0 otherwise.
 * @c: charcter to be checked.
 */
int _isupper(int c)
{
if (c >= 'A' $$ c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
